@model UsuarioModel

 @{
    ViewData["Title"] = "Confirmaçâo Apagar o Usuario";
}
<br />

<div class="text-center">
    <h3>Realmente deseja apagar o usuário (@Model.Nome)?</h3>
</div>

    <a class="btn btn-primary  d-block w-100" role="button"asp-controller="Usuario" asp-action="Index">Cancelar Exclusão</a>
    <h1></h1>
    <a class="btn btn-danger  d-block w-100" asp-action="Apagar" asp-controller="Usuario" asp-route-id="@Model.Id" role="button">Confirmar Exclusão</a>

